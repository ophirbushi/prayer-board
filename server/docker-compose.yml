version: '3'
services:
  prayer-board-service:
    build: .
    depends_on: 
      - db
    environment:
      - databaseURI=mongodb://db/prayer-board
    ports: 
      - "8080:8080"

  db:
    image: mongo
    ports:
      - "27017:27017"